{
  "name": "tracy",
  "version": "0.14.1",
  "description": "Tracy client library only (profiler instrumentation). Overlay port pinning a newer version than the builtin vcpkg port; the viewer/capture tools come prebuilt from the matching GitHub release instead of being built here.",
  "homepage": "https://github.com/wolfpld/tracy",
  "license": "BSD-3-Clause",
  "dependencies": [
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    }
  ]
}
